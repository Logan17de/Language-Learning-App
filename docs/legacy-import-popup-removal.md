# Legacy import popup removal

The automatic `LegacyImportAssistant` is no longer mounted by the root layout.
Existing local-to-Supabase migration helpers remain available in the codebase, but signed-in learners are no longer interrupted by an unsolicited progress-import prompt.
